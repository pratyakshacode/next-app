/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['img.freepik.com'],
        
    },

    // this is neccessary for creation of static html documents
}

module.exports = nextConfig
